// Question no: 22

// Array of fruits
const fruits: string[] = ["apple", "banana", "orange"];

// Try to access an index that doesn't exist (index 3)
console.log(fruits[3]); // This will cause an index error

// To correct the error, we can either access an existing index or adjust the index
console.log(fruits[2]); // Corrected index access
